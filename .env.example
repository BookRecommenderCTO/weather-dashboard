# Weather Dashboard Environment Variables

# OpenWeatherMap API Key (get from https://openweathermap.org/api)
OPENWEATHER_API_KEY=your_api_key_here

# Database URL (PostgreSQL connection string)
# For local development, you can use a local PostgreSQL instance
# DATABASE_URL=postgresql://username:password@localhost:5432/weather_dashboard
# For Render, this will be automatically set by the database service
DATABASE_URL=

# Node Environment
NODE_ENV=development

# Port (Render will set this automatically in production)
PORT=3000
